package com.allcenter.moduletransport.model;

/** Acciones registradas para trazabilidad de transporte (similar semántica a audit empleados). */
public enum TransportAuditAction {
    CREATE,
    UPDATE,
    DELETE
}
