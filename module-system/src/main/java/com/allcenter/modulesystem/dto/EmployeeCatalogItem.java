package com.allcenter.modulesystem.dto;

/** Entrada de catálogo para desplegables (p. ej. choferes con rol CHOFER). */
public record EmployeeCatalogItem(Long id, String email, String displayName) {}
