# Valores por defecto (sobrescribibles por módulo o perfil)
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:5173,http://127.0.0.1:5173,http://localhost:4173}
app.security.api-auth-enabled=${APP_SECURITY_API_AUTH_ENABLED:false}
app.security.headers-enabled=${APP_SECURITY_HEADERS_ENABLED:true}
app.security.validate-secrets-on-startup=${APP_SECURITY_VALIDATE_SECRETS:false}
